# Claude Code local instructions & config — not for the public repo
CLAUDE.md
.claude/

# Python
__pycache__/
*.pyc
.venv/

# secrets: ignore every .env variant, keep the checked-in template
.env*
!.env.example

# local run artifacts
*.log
.vercel

# repo-in-repo archive, not for a public repo
*.zip

# cluster CA: supplied via the COCKROACH_CA_PEM env var instead
certs/

# demo video capture output — local only, never pushed
capture/
*.mp4
*.webm
